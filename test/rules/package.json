{
  "name": "jj-playbook-rules-test",
  "private": true,
  "description": "Checks firestore.rules in the local Firestore emulator (project demo-jj, never a real project).",
  "scripts": {
    "test": "firebase emulators:exec --project demo-jj --only firestore \"node rules.test.js ../../firestore.rules\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "3.0.4",
    "firebase": "10.14.1",
    "firebase-tools": "13.35.1"
  }
}
